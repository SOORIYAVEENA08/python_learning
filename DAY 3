'''
problem 1:
Given two strings S1 and S2 as input. Your task is to concatenate two strings and then reverse the string. Finally print the reversed string.
ex:
Input: S1 = "Geeks" , S2 = "forGeeks"
Output: "skeeGrofskeeG" 
Explanation: Concatenating S1 and S2 to 
get "GeeksforGeeks" then reversing it to 
"skeeGrofskeeG".

#solution
class sloution():
    def reversed_string(self,s1,s2):
        s=s1+s2
        reversed__string=''.join(reversed(s))   #we use join because reversed() method is a iterator it can directly print the reversed string in order to converrt the iterator into string we use join 
        return reversed__string
s1="geeksfor"
s2="geeks"
print(sloution().reversed_string(s1,s2))

#output:skeegrofskeeg
